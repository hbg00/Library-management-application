@using BookStore.ViewModels
@model IEnumerable<Publisher>
@{
    var counter = 0;
}

<div class="seraching-bar-user">
    <form asp-controller="Publisher" asp-action="FilterPublishers" method="get">
        <input name="searchedPhrase" />
        <input type="submit" />
    </form>
</div>
<div class="data">
    @if (Model != null)
    {
        <table>
            @foreach (var publisher in Model)
            {
                counter++;
                <tr>
                    <td>@counter</td>
                    <td>@publisher.FirstName</td>
                    <td>@publisher.LastName</td>
                    <td><a asp-controller="Publisher" asp-action="Edit" asp-route-id="@publisher.Id">Edit</a></td>
                </tr>
            }
        </table>
    }
    else
    {
        <p>There are no publishers in the database yet.</p>
    }
</div>